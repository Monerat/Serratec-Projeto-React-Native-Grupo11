import { View } from "react-native";
import { PokemonCard } from "../../components/PokemonCard";

export const CardScreen = () => {
      
    return (
      <View>
        <PokemonCard/>
      </View>
    );
  };
